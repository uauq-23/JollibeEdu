Local git author for this project
- user.name = uauq23
- user.email = hoangquau@gmail.com

If you already created a GitHub repository named JollibeEdu:

git init
git branch -M main
git remote add origin https://github.com/uauq23/JollibeEdu.git
git add .
git commit -m "Initial upload of JolibeeEdu project"
git push -u origin main

If origin already exists:

git remote set-url origin https://github.com/uauq23/JollibeEdu.git
git add .
git commit -m "Update JolibeeEdu project"
git push -u origin main

If you want a different repository name, replace JollibeEdu in the URL above.
